#include <bits/stdc++.h>
#define LL long long

using namespace std;

string s;

int main() {
  cin.tie(0), cout.tie(0);
  std::ios::sync_with_stdio(false);
  while (cin >> s) {
    cout.flush() << "NO\n";
  }
  return 0;
}
