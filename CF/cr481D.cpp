#include <bits/stdc++.h>
#define LL long long

using namespace std;

int n, a[100001];

int main() {
  cin.tie(0), cout.tie(0);
  ios::sync_with_stdio(false);
  cin >> n;
  for (int i = 1; i <= n; i++) {
    cin >> a[i];
  }
  for (int i = -1; i <= 1; i++) {  //枚举a[1],a[2]
    for (int j = -1; j <= 1; j++) {
      
    }
  }
  return 0;
}
