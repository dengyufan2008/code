#include <bits/stdc++.h>
#define LL long long

using namespace std;

int t, n, a[2001];

int main() {
  cin.tie(0), cout.tie(0);
  ios::sync_with_stdio(false);
  cin >> t;
  while (t--) {
    cin >> n;
    for (int i = 1; i <= n; i++) {
      cin >> a[i];
    }
    
  }
  return 0;
}
