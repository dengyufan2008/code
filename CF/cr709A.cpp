#include <bits/stdc++.h>
#define LL long long

using namespace std;

int t, a, b;

int main() {
  cin.tie(0), cout.tie(0);
  std::ios::sync_with_stdio(false);
  cin >> t;
  while(t--) {
    cin >> a >> b;
    cout << a * b << "\n";
  }
  return 0;
}
