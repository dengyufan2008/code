#include <fstream>

using namespace std;

ifstream cin("l.in");
ofstream cout("l.out");

const int kMaxN = 2e5 + 1, kMod = 998244353;
int n, m, q;

int main() {
  cin.tie(0), cout.tie(0);
  ios::sync_with_stdio(0);
  cin >> n >> m >> q;
  ;
  return 0;
}
