#include <ctime>
#include <iostream>
#define LL long long

using namespace std;

int main() {
  // freopen("cheappal.in", "r", stdin);
  // freopen("cheappal.out", "w", stdout);
  
  // cout << "Runtime:" << (double)clock() / 1000.0 << "s" << endl;
  return 0;
}
