#include <ctime>
#include <iostream>
#define LL long long

using namespace std;

int main() {
  // freopen("jumpcow.in", "r", stdin);
  // freopen("jumpcow.out", "w", stdout);
  
  // cout << "Runtime:" << (double)clock() / 1000.0 << "s" << endl;
  return 0;
}
