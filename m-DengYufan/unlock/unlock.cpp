#include <bits/stdc++.h>
#define LL long long

using namespace std;

int main() {
  // freopen("unlock.in", "r", stdin);
  // freopen("unlock.out", "w", stdout);
  cin.tie(0), cout.tie(0);
  ios::sync_with_stdio(false);
  
  return 0;
}
