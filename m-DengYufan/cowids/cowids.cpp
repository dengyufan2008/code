#include <ctime>
#include <iostream>
#define LL long long

using namespace std;

int n, k;

int main() {
  // freopen("cowids.in", "r", stdin);
  // freopen("cowids.out", "w", stdout);
  cin >> n >> k;
  
  // cout << "Runtime:" << (double)clock() / 1000.0 << "s" << endl;
  return 0;
}
