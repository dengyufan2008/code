#include <ctime>
#include <iostream>
#define LL long long

using namespace std;

int main() {
  // freopen("eat.in", "r", stdin);
  // freopen("eat.out", "w", stdout);
  
  // cout << endl << "Runtime:" << (double)clock() / 1000.0 << "s" << endl;
  return 0;
}
