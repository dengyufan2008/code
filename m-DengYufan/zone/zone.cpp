#include <ctime>
#include <iostream>
#define LL long long

using namespace std;

int main() {
  // freopen("zone.in", "r", stdin);
  // freopen("zone.out", "w", stdout);
  
  // cout << "Runtime:" << (double)clock() / 1000.0 << "s" << endl;
  return 0;
}
