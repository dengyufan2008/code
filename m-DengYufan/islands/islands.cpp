#include <bits/stdc++.h>
#define LL long long

using namespace std;

int n, h[100001];

int main() {
  // freopen(".in", "r", stdin);
  // freopen(".out", "w", stdout);
  cin.tie(0), cout.tie(0);
  ios::sync_with_stdio(false);
  cin >> n;
  for (int i = 1; i <= n; i++) {
    cin >> h[i];
  }
  return 0;
}
