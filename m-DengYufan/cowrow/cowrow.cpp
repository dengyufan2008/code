#include <bits/stdc++.h>
#define LL long long

using namespace std;

int n, a[1001];

int main() {
  // freopen("cowrow.in", "r", stdin);
  // freopen("cowrow.out", "w", stdout);
  cin.tie(0), cout.tie(0);
  ios::sync_with_stdio(false);
  cin >> n;
  for (int i = 1; i <= n; i++) {
    cin >> a[i];
  }
  
  return 0;
}
