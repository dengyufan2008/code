#include <bits/stdc++.h>
#define LL long long

using namespace std;

int main() {
  // freopen("3lines.in", "r", stdin);
  // freopen("3lines.out", "w", stdout);
  cin.tie(0), cout.tie(0);
  ios::sync_with_stdio(false);
  
  return 0;
}
