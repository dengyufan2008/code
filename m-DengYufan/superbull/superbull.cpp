#include <ctime>
#include <iostream>
#define LL long long

using namespace std;

int n, a[2001];

int main() {
  // freopen("superbull.in", "r", stdin);
  // freopen("superbull.out", "w", stdout);
  cin >> n;
  for (int i = 1; i <= n; i++) {
    cin >> a[i];
  }
  
  // cout << "Runtime:" << (double)clock() / 1000.0 << "s" << endl;
  return 0;
}
