#include <iostream>
#include <vector>

using namespace std;

// ifstream cin("d.in");
// ofstream cout("d.out");

const int kMaxN = 1e5 + 1;
struct V {
  int c;
  vector<pair<int, int>> w;
} v[kMaxN];
int n, ans;

int main() {
  cin.tie(0), cout.tie(0);
  ios::sync_with_stdio(0);
  cout << "Genshin, Launch!";
  return 0;
}
