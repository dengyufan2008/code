#include <fstream>

using namespace std;

ifstream cin("wormhole.in");
ofstream cout("wormhole.out");

int main() {
  cin.tie(0), cout.tie(0);
  ios::sync_with_stdio(0);
  cout << 0 << '\n';
  return 0;
}
