#include <iostream>
#define LL long long

using namespace std;

struct V {
  int v, l, r;
} v[4000001];
int n, m, s[1000001] = {1};

int main() {
  cin >> n >> m;
  for (int i = 1, x; i <= n; i++) {
    cin >> x;
    Insert(s[0])
  }
  return 0;
}
