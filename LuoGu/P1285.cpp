#include <iostream>
#include <vector>
#define LL long long

using namespace std;

struct V {
  vector<int> e;
} v[101];
int n;
bool b[101];

int main() {
  cin >> n;
  for (int i = 1, x; i <= n; i++) {
    fill(&b[0], &b[100] + 1, 0);
    
  }
  return 0;
}
